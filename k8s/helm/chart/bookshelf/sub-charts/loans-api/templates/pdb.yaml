{{- if gt (int .Values.minPodsAvailable) 0 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "loans-api.fullname" . }}
  labels:
    {{- include "loans-api.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.minPodsAvailable }}
  selector:
    matchLabels:
      {{- include "loans-api.selectorLabels" . | nindent 6 }}
{{- end }}
